method: grid
metric:
  name: train_loss
  goal: minimize
parameters:
  learning_rate:
    values: [0.01]
  batch_size:
    values: [250, 500]
  epochs: 
    value: 3
  optimizer: 
    values: ["sgd"]
  dataset_name: 
    values: ["mnist"]
  model_name: 
    values: ["simple_resnet"]
  criterion:
    values: ["cross_entropy"]
